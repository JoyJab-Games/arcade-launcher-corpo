[package]
name = "launcher_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]   # wichtig: dynamische Library

[dependencies]
godot = "0.4"             # gdext
zbus = "5"                # später für NetworkManager/udisks2
